


#pecanLoops <- function(nodes,edges){
  # Enable list of feedback_loos
#  feedback_loops <- list()
  # Transform PECAN dfs into igraph object
#  gfl <- pecan2igraph(nodes = nodes, edges = edges)
  # Perform DFS from each unvisited node in the graph
#  num_nodes <- vcount(gfl) # calculate number of nodes
#  visited_nodes <- rep(FALSE, num_nodes) # enable vecot of nodes that have been visited

#  for (node in 1:num_nodes) {
#    if (!visited_nodes[node]) {
##      find_cycles(node = node, visited_nodes = visited_nodes, path = integer(0))
#    }
#  }


#  }
#
